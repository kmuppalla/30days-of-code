int x;
	double y;
	string z;// Declare second integer, double, and String variables.

	
	cin >> x;
	cin >> y;
	std::cout << std::setprecision(1) << std::fixed;
    cin.ignore();
    getline(cin, z);
	


	int sum = i + x;
	cout << sum << endl;
	double sum1 = d + y;
	cout << sum1 << endl;
	cout << s << z << endl;

    return 0;
